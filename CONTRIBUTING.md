Commit focused changes with a short explanation of the skill-index behavior they affect.

Before opening a pull request, run:

```sh
npm test
npm run check
npm run smoke
npm run release:check
```

Keep fixture skills small and deterministic so warning counts remain easy to review.
